# ── Twenty CRM connection ────────────────────────────────────────────
# Base URL of your self-hosted Twenty instance (no trailing slash)
TWENTY_API_URL=http://localhost:3000

# API key – generate in Settings → APIs & Webhooks → + Create key
TWENTY_API_KEY=your-api-key-here

# ── Excel file ───────────────────────────────────────────────────────
# Absolute or relative path to the .xlsx file (created automatically)
# EXCEL_FILE_PATH=./twenty_crm_data.xlsx

# ── Sync behaviour ──────────────────────────────────────────────────
# Conflict resolution: "crm_wins" | "excel_wins" | "newest_wins"
CONFLICT_STRATEGY=newest_wins

# Rate-limit delay between API calls (seconds)
API_RATE_LIMIT_DELAY=0.7

# Max records per batch API call (Twenty limit = 60)
BATCH_SIZE=60

# ── Scheduler ────────────────────────────────────────────────────────
# Interval in minutes when running `python main.py schedule`
SYNC_INTERVAL_MINUTES=30

# ── LinkedIn (Member Data Portability API) ───────────────────────────
# Create an app at https://www.linkedin.com/developers/apps
# using the "Member Data Portability (Member) Default Company" page,
# then request access to "Member Data Portability API (Member)".
LINKEDIN_CLIENT_ID=your-linkedin-client-id
LINKEDIN_CLIENT_SECRET=your-linkedin-client-secret
LINKEDIN_REDIRECT_URI=http://localhost:8787/callback
LINKEDIN_SCOPE=r_dma_portability_self_serve

# Access token – paste from LinkedIn's OAuth Token Generator Tool
# (alternative to the browser-based OAuth flow)
LINKEDIN_ACCESS_TOKEN=

# Snapshot domains to pull (comma-separated)
# Options: PROFILE, CONNECTIONS, POSITIONS, EDUCATION, SKILLS, etc.
LINKEDIN_SNAPSHOT_DOMAINS=PROFILE,CONNECTIONS
